import collectedRewards from './collectedRewards';
import common from './common';
import home from './home';

export const fr = {
  home,
  common,
  collectedRewards
};

export default fr;
export type TranslationsType = typeof fr;
