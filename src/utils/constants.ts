import type { Edge } from 'react-native-safe-area-context';

import { colors } from './colors';

export const SAFE_AREA_EDGES: Edge[] = ['right', 'left'];

export const STATUS_BAR_STYLE = 'dark-content';

export const AppLanguages = {
  en: 'en',
  fr: 'fr'
};

export const TOAST_DURATION = 6000;

export const DEFAULT_ROWS_PER_PAGE = 10;

export const BOTTOM_BAR_BACKGROUND_COLOR = colors.black;
export const BOTTOM_BAR_ACTIVE_COLOR = colors.primaryPurple;
export const BOTTOM_BAR_INACTIVE_COLOR = colors.white;

export const REWARDS_PER_PAGE = 15;
